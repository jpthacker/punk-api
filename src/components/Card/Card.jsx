import React from "react";
import styles from "./Card.module.scss";

const Card = () => {
  return (
    <>
      <section className={styles.beerCard}>Card works</section>
    </>
  );
};

export default Card;
